// Central place for the four recurring post formats, so the label shown
// on the site always matches the `format` value used in post frontmatter.
export const FORMAT_LABELS: Record<string, string> = {
  letter: 'A note to a first-time lead',
  mistake: 'One I got wrong',
  'built-upon': 'Built upon',
  tool: 'Monday tool',
};

export const THEME_LABELS: Record<string, string> = {
  'leading-self': 'Leading self',
  'leading-others': 'Leading others for the first time',
};
